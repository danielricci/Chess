language: java

jdk: oraclejdk8

android:
  components:
    - gradle-wrapper

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

before_script:
  - cd Chess
  - chmod +x gradlew

deploy:
  provider: releases
  api_key: "2a1722e71dd57e722e64b78520dc7582e64a2138 "
  file: "build/libs/Chess.jar"
  skip_cleanup: true
  on:
    tags: true
